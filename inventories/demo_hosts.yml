all:
  children:
    # ! DEMO
    demo:
      hosts:
        demo-pve:
          ansible_host: 10.0.5.10
          ansible_user: root
          ansible_ssh_common_args: '-o IdentitiesOnly=yes'
        demo-vm09-aio:
          ansible_host: 10.0.90.9
          ansible_user: wasd
          ansible_ssh_common_args: '-o IdentitiesOnly=yes'
        demo-vm10-ingress:
          ansible_host: 10.0.90.10
          ansible_user: wasd
          ansible_ssh_common_args: '-o IdentitiesOnly=yes'
        demo-vm20-metrics:
          ansible_host: 10.0.90.20
          ansible_user: wasd
          ansible_ssh_common_args: '-o IdentitiesOnly=yes'
        demo-vm31-application:
          ansible_host: 10.0.90.31
          ansible_user: wasd
          ansible_ssh_common_args: '-o IdentitiesOnly=yes'

# ! Notes:
# The private_key will be set through variables. Therefore, set it there.
# Also as a Hint: id_ecdsa_sk like yubikey requires touch!
