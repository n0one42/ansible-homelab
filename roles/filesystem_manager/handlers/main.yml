---
# handlers file for roles/filesystem_manager
- name: Restart container => {{ filesystem_manager_app_name }}
  listen: "restart_container"
  community.docker.docker_container:
    name: "{{ filesystem_manager_app_name }}"
    state: started
    restart: true
  when: filesystem_manager_restart_container_on_template_change | default(true) and container_info.exists
